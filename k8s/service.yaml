apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: default          # <-- yeh line add kar
spec:
  type: NodePort
  selector:
    app: flask
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30000